`NotoSansSC-Regular.ttf` is bundled for server-side export rendering so PNG/PDF output does not depend on the host machine having Chinese fonts installed.

The runtime can be overridden with `AI_EXPORT_FONT_PATH` when a different font file should be used on the server.
